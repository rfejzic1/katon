let person = {
    let name = 'John Doe';

    printName() {
        print(name);
    }
};

doSomething(something) {
    something.do();
}

main() {
    doSomething(person) otherwise {
        print("This has gone out of hand, hasn't it?");
    };
}
